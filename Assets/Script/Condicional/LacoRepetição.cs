using UnityEngine;

public class LacoRepetição : MonoBehaviour

    //For --> É utilizado quando você sabe a qtd. de repetições.
    //While --> É utilizado quando não se sabe a qtd. de repetições.
    //Foreach --> Usado em listas e arrays, para executar uma operação para cada item da lista

    //Array --> É uma lista

{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //for(inicialização
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
